# Patch UI v0.2

Décompresser ce ZIP à la racine du dépôt `couple` en remplaçant les fichiers existants.

Aucune nouvelle dépendance n'est nécessaire pour ce patch UI.
Une fois les fichiers remplacés, Expo/Metro doit recharger automatiquement l'application.
